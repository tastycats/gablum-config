server:
  port: ${APP_SERVER_PORT}

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${REGISTRY_URI}

spring:
  application:
    name: "auctions-service"
  data:
    mongodb:
      uri: ${AUCTIONS_DB_URI}
